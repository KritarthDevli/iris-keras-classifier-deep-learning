# iris-keras-classifier-deep-learning
This project implements a neural network classifier for the Iris dataset using TensorFlow and Keras. The model uses feature normalization, dense layers, and softmax classification to predict flower species. It includes data loading, one-hot encoding, training, loss visualization, and accuracy evaluation.
